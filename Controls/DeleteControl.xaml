<UserControl x:Class="UI.Code.Controls.DeleteControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
              xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:UI.Code.Controls"
             mc:Ignorable="d" 
            >
    <Button  Padding="5" Visibility="Visible" Foreground="Red" Command="{Binding DeleteCommand}"  HorizontalAlignment="Right" VerticalAlignment="Bottom"
  Style="{StaticResource ImgBtn}" 
  ToolTip="Delete">
        <materialDesign:PackIcon Foreground="White"
    Kind="DeleteCircle"
    
     />
    </Button>
</UserControl>
